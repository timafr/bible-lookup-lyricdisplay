name: Build Multi-Platform Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  quality:
    name: Static and unit checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install native module build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libsecret-1-dev libasound2-dev

      - name: Install dependencies
        run: npm ci

      - name: Install server dependencies
        run: npm ci
        working-directory: server

      - name: Run static checks
        run: npm run check:static

      - name: Run unit tests
        run: npm run test:unit

  build-windows:
    needs: quality
    runs-on: windows-2022
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: npm ci

      - name: Install server dependencies
        run: |
          cd server
          npm ci

      - name: Audit production dependencies
        run: |
          npm run audit:prod
          npm run audit:server

      - name: Rebuild keytar native module for Windows
        run: npm rebuild keytar --build-from-source

      - name: Build Vite app
        run: npm run build

      - name: Build Windows installer
        run: npx electron-builder --win --publish never

      - name: Smoke test packaged Windows runtime
        run: node scripts/verify-packaged-runtime.js --windows --start-server

      - name: Verify Windows blockmaps
        run: node scripts/verify-blockmaps.js --windows

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v7
        with:
          name: windows-installer
          path: |
            release/*.exe
            release/*.blockmap
            release/latest.yml
          retention-days: 5

  build-macos:
    needs: quality
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: npm ci

      - name: Install server dependencies
        run: |
          cd server
          npm ci

      - name: Audit production dependencies
        run: |
          npm run audit:prod
          npm run audit:server

      - name: Rebuild keytar native module for macOS
        run: npm rebuild keytar --build-from-source

      - name: Build Vite app
        run: npm run build

      - name: Build macOS installers
        run: npx electron-builder --mac --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Smoke test packaged macOS runtime
        run: node scripts/verify-packaged-runtime.js --macos --start-server

      - name: Verify macOS blockmaps
        run: node scripts/verify-blockmaps.js --macos

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v7
        with:
          name: macos-installer
          path: |
            release/*.dmg
            release/*.zip
            release/*.blockmap
            release/latest-mac.yml
          retention-days: 5

  build-linux:
    needs: quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libsecret-1-dev rpm libasound2-dev

      - name: Install dependencies
        run: npm ci

      - name: Install server dependencies
        run: |
          cd server
          npm ci

      - name: Audit production dependencies
        run: |
          npm run audit:prod
          npm run audit:server

      - name: Rebuild keytar native module for Linux
        run: npm rebuild keytar --build-from-source

      - name: Build Vite app
        run: npm run build

      - name: Build Linux installers
        run: npx electron-builder --linux --publish never

      - name: Smoke test packaged Linux runtime
        run: node scripts/verify-packaged-runtime.js --linux --start-server

      - name: Verify Linux blockmaps
        run: node scripts/verify-blockmaps.js --linux

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v7
        with:
          name: linux-installer
          path: |
            release/*.AppImage
            release/*.deb
            release/*.rpm
            release/*.blockmap
            release/latest-linux.yml
          retention-days: 5

  create-release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist-artifacts

      - name: Display structure of downloaded files
        run: ls -R dist-artifacts

      - name: Extract version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Read release notes from commit
        id: release_notes
        run: |
          git log -1 --pretty=%B > commit_msg.txt
          
          if grep -q "RELEASE_NOTES_START" commit_msg.txt; then
            echo "CUSTOM_NOTES<<EOF" >> $GITHUB_OUTPUT
            sed -n '/RELEASE_NOTES_START/,/RELEASE_NOTES_END/p' commit_msg.txt | sed '1d;$d' >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            echo "Found release notes in commit message"
          else
            echo "CUSTOM_NOTES=" >> $GITHUB_OUTPUT
            echo "No release notes found in commit message"
          fi
          
          rm -f commit_msg.txt

      - name: Create or update release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          name: v${{ steps.get_version.outputs.VERSION }}
          draft: false
          prerelease: false
          files: |
            dist-artifacts/windows-installer/*
            dist-artifacts/macos-installer/*
            dist-artifacts/linux-installer/*
          fail_on_unmatched_files: false
          body: |
            ## LyricDisplay ${{ steps.get_version.outputs.VERSION }}
            
            ${{ steps.release_notes.outputs.CUSTOM_NOTES }}
            
            ---

            ### Downloads
            
            **Windows:**
            - **Windows 10/11 (64-bit)**: Download `LyricDisplay-${{ steps.get_version.outputs.VERSION }}-Windows-Setup.exe`
            
            **macOS:**
            - **Apple Silicon (M1/M2/M3)**: Download `LyricDisplay-${{ steps.get_version.outputs.VERSION }}-macOS-arm64.dmg`
            - **Intel Macs**: Download `LyricDisplay-${{ steps.get_version.outputs.VERSION }}-macOS-x64.dmg`
            
            **Linux:**
            - **AppImage** (recommended): `LyricDisplay-${{ steps.get_version.outputs.VERSION }}-Linux.AppImage` - Works on all distributions
            
            ### Installation
            
            See the [Installation Guide](https://lyricdisplay.app/integration-guide) for detailed setup instructions.
            
            ---
            
            ### ⚠️ macOS Users: Important Installation Steps
            
            Because this app is not code-signed with an Apple Developer certificate, macOS will show a "damaged" or "can't be opened" error. **This is expected** and can be easily fixed:
            
            1. Download and open the `.dmg` file, drag LyricDisplay to Applications
            2. **Before opening the app**, open Terminal and run:
               ```
               xattr -cr /Applications/LyricDisplay.app
               ```
            3. Now open LyricDisplay from Applications
            4. If prompted, go to **System Preferences → Security & Privacy → General** and click "Open Anyway"
            
            You only need to do this once. After the first launch, macOS will remember your choice.
            
            ---
            
            ### Windows & Linux Notes
            
            **Windows:** If SmartScreen appears, click "More info" → "Run anyway"
            
            **Linux:** Make the AppImage executable before running:
            ```
            chmod +x LyricDisplay-*.AppImage
            ./LyricDisplay-*.AppImage
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
